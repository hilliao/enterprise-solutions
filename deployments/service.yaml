apiVersion: v1
kind: Service
metadata:
  name: hello-java
  labels:
    svc: hello-java
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8080
  selector:
    app: hello-java
  type: LoadBalancer